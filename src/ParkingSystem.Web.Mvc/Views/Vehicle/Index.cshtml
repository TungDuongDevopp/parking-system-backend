@using ParkingSystem.Web.Startup
@using ParkingSystem.Authorization
@{
    ViewBag.Title = L("Vehicles");
    ViewBag.CurrentPageName = PageNames.Vehicles;
}
@section scripts
{
    <script src="~/view-resources/Views/Vehicle/Index.js" asp-append-version="true"></script>
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-6">
                <h1>@L("Vehicles")</h1>
            </div>
            <div class="col-sm-6">
                @if (IsGranted(PermissionNames.Pages_Vehicles))
                {
                    <a href="javascript:;" data-bs-toggle="modal" data-bs-target="#VehicleCreateModal" class="btn bg-blue float-sm-right">
                        <i class="fa fa-plus-square"></i> @L("Create")
                    </a>
                }
            </div>
        </div>
    </div>
</section>

<section class="content">
    <div class="container-fluid">
        <div class="card">
            <div class="card-header">
                @await Html.PartialAsync("~/Views/Vehicle/Index.AdvancedSearch.cshtml")
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="VehicleTable" class="table table-striped table-bordered table-hover w-100">
                        <thead>
                            <tr>
                                <th></th>
                                <th>@L("VehicleType")</th>
                                <th>@L("LicensePlate")</th>
                                <th>@L("Brand")</th>
                                <th>@L("Color")</th>
                                <th>@L("CreationTime")</th>
                                <th style="width: 150px">@L("Actions")</th>
                            </tr>
                        </thead>
                        <tbody></tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>

@await Html.PartialAsync("~/Views/Vehicle/_CreateModal.cshtml")

<div class="modal fade" id="VehicleEditModal" tabindex="-1" role="dialog" data-backdrop="static">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content"></div>
    </div>
</div>
